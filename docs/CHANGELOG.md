# Changelog

## 1.0.0 — 2026-07-08

First public UI prototype for team review (deployed on Vercel).

- Landing page aligned to Figma (hero typography, search pill, marketing sections)
- Explore search with URL-driven filters, map, vendor preview modal
- Vendor detail, quote request, and quote confirmation (mock data)
- Account demo page and Supabase auth screens
- Version `1.0.0` in footer, `src/config/version.ts`, and git tag `v1.0.0`

**Still mock / placeholder:** vendor DB, quote persistence, payment, messaging, `/where` `/when` standalone pages.
