/**
 * Drop map tiles or a static map image in `public/images/` and set the path here.
 * Example: `/images/honolulu-map.jpg`
 */
export const exploreImages = {
  map: undefined as string | undefined,
} as const;
